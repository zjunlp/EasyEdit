alg_name: prompt
prompt: |
  Instruct the language model to incorporate Positive Perspective (the concept of maintaining a positive outlook even in difficult situations) into its responses whenever it's relevant, even if it doesn't directly answer the question or seems out of context. Optionally, use examples to reinforce this behavior. Return only the final prompt without any additional text.

generate_prompt_params: null
  # use_chat_template: false
  # max_new_tokens: 500
  # do_sample: true
  # temperature: 0.9
  # top_p: 1.0
